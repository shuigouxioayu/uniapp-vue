import API from '@/utils/api'
import {getUsers,addUserInfo} from '@/service.js';

const state = {
	userInfo: null,
	userToken: null,
	
}

const mutations = {
	
}

const actions = {
	
}

export default {
	namespaced: true,
	state,
	mutations,
	actions
}
